# Disposable local Supabase project for the M050 runtime harness only.
# This file intentionally contains no hosted project reference, URL, or secret.
project_id = "aadhyant-m050-local-validation"

[api]
enabled = true

[auth]
enabled = true
site_url = "http://127.0.0.1:3000"
additional_redirect_urls = ["http://127.0.0.1:3000"]

[storage]
enabled = true
