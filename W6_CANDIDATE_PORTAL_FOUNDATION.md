# W6 Candidate Portal and Document Onboarding Foundation

Status: COMPLETE locally with no remaining technical blocker. Migrations 023, 024, and 025 are installed and validated on dedicated NONPROD staging. Checkpoints 025-028, required W2-W5 and legacy regressions, 121/121 frontend tests, live browser validation including the focused Admin review-start retest, and final manifest-bound zero-residue cleanup pass. Production was never contacted, no deployment occurred, and W7 was not started. The implementation was reviewed through local HEAD `ddf55133bf0c44c2e4817ba71924cad71819f6b2` before this documentation-only reconciliation commit. Normal non-force push remains separately authorized work; remote closure occurs only after push and remote HEAD verification.

## Canonical architecture

W6 extends the existing `candidates`, `candidate_preferences`, `platform_users`, `employer_requirements`, `candidate_applications`, `application_stage_history`, `interviews`, `candidate_joinings`, and `audit_logs` contracts. It does not create a second candidate, job, application, interview, or joining system. The separate authenticated route is `/candidate/portal/`; the existing no-login public registration/interest flow remains available and is not silently linked to Auth accounts.

Protected Candidate scope resolves through `auth.uid()` to one active `platform_users` candidate account and exactly one active canonical `candidates` row. Inactive, conflicting, and ambiguous linkage fails closed. Company, Contractor, internal staff, other candidates, and anonymous callers receive no protected Candidate scope merely because they are authenticated. A first-time authenticated user with no conflicting Candidate, Company, Contractor, Admin, or staff linkage is intentionally eligible for the generic Candidate onboarding flow; no existing Candidate data is disclosed before onboarding succeeds.

## Required profile and sensitive identifiers

Mobile and Aadhaar are compulsory before a candidate profile is complete or a portal application can be created. Indian mobile numbers normalize to ten digits and must start from 6-9. Aadhaar normalizes to exactly twelve digits. Full Aadhaar is not retained: the database stores a SHA-256 equality fingerprint and last four digits, and portal reads return only `XXXX XXXX 1234`. The fingerprint provides deterministic uniqueness checking but is not encryption and should not be treated as protection against offline enumeration if database contents are compromised. The repository has no reviewed server-side key-management/HMAC facility, so W6 does not invent one or expose a key to browser code. A key-managed HMAC/tokenization review is a production-readiness requirement before collecting real Aadhaar; it does not block rollback-scoped NONPROD W6 runtime validation.

Mobile verification defaults to unverified. W6 does not fake OTP verification; real OTP and verification-state mutation are deferred. Generic duplicate errors avoid disclosing whether another candidate owns a mobile or Aadhaar value. Full Aadhaar, bank account, UAN, and ESIC values are excluded from URLs, lists, client storage, and audit metadata.

Profile completion is calculated server-side across required identity/contact, education, location, preferences, and Resume categories. Joining-document completion is separate. Bank details retain only a deterministic fingerprint and last four account digits; UAN and ESIC values are stored only when the candidate says an existing number is available and are projected masked. W6 never generates or validates these identifiers against external government systems.

## Private documents

`candidate_documents` is a normalized metadata entity with controlled type, private object name, safe display name, MIME type, size, active/replacement state, and verification state. One active record per candidate/type is enforced. Its controlled categories cover Resume/CV, Aadhaar, PAN, photo, 10th/12th, ITI, Diploma, Degree and other education, experience/previous employment, bank proof/passbook/cancelled cheque, driving licence, passport, and Other. Supported uploads are declared PDF, JPEG, and PNG up to 10 MB. The `candidate-private` Storage bucket is private. Candidate self-service requires both an active, unambiguous Candidate linkage and an object path owned by the same Auth UID. Authorized Bootstrap Admin, Super Admin, and Admin verification access is a separate SELECT-only policy; Company, Contractor, Recruiter, Operations, unrelated Candidate, and anonymous access is denied. Object names, declared MIME, extension, and recorded size are server-validated before metadata registration. This is not magic-byte inspection, malware scanning, or content quarantine; those controls remain required before production document intake. Candidate and authorized Admin views use short-lived signed access and never render raw paths or public URLs.

Verification states are Uploaded, Under Verification, Verified, and Re-upload Required. Bootstrap Admin, Super Admin, and Admin may inspect and review documents. The Admin UI mirrors the server state graph exactly: Uploaded rows expose View Securely and Start Review; Under Verification rows expose View Securely, Mark Verified, and Request Re-upload; Verified and Re-upload Required rows expose View Securely only. Start Review invokes the existing review RPC with `under_verification`, and every successful transition reloads the canonical server projection rather than applying frontend-only state. Recruiter and Operations are intentionally excluded from Aadhaar/document verification in W6. Company and Contractor roles have no document or Aadhaar projection. Re-upload feedback is bounded and candidate-facing. Resume remains Candidate/Aadhyant controlled; Company/Contractor release requires a separate consent and stage policy.

The server-derived joining checklist includes Mobile, Aadhaar number/document, Candidate Photo, bank details/proof, and conditional supporting categories. Verified and missing states are not fabricated. A privileged documentation override requires a bounded reason, actor, and timestamp and does not mark documents Verified.

## Opportunities and recruitment flow

Job Opportunities projects only canonical requirements that are Open, Public, have remaining openings, and have a requirement code. Contractor Draft, Submitted, Under Review, Correction Required, Rejected, private, filled, and closed requirements are absent. Candidate identity is server-derived when applying. `apply_candidate_job` inserts one canonical `candidate_applications` row at Applied and the existing unique candidate/requirement constraint prevents duplicates. W3 remains authoritative for application transitions, interview scheduling/outcomes, and joining mutation.

Candidate reads are limited to own applications, safe interview schedules/results, and joining progress. No Candidate Master, other candidate, internal stage-history reason, recruiter note, internal note, audit record, Company private note, or Contractor private detail is projected. Confirmed/Expected joining remains visibly distinct from Actually Joined.

## Frontend and session safety

The responsive portal includes Dashboard, My Profile, Job Opportunities, My Applications, Interviews, Joining Status, Documents, and Logout. Forms use labels, native structured controls, status regions, keyboard-accessible navigation, bounded tables, and mobile breakpoints. It contains no operational `prompt()`, `alert()`, UUID entry, raw status/storage-path entry, or direct business-table access.

Protected content is concealed before logout and on `pagehide`. `pageshow` detects BFCache or back/forward restoration, conceals stale content, and reloads so the server-backed session/context gate runs again. This incorporates the W5 history-cache remediation from the beginning.

## RPC and role surface

Private helpers: `current_candidate_portal_id` and `can_verify_candidate_documents`.

Candidate RPCs cover context, initial profile creation, profile read/update, dashboard metrics, eligible jobs, application creation/list, interview/joining lists, document inventory/registration/access, joining checklist, and masked bank/PF/ESIC onboarding read/update.

Admin RPCs cover document inventory/access, review start, verification/re-upload review, and controlled documentation override. Every W6 helper/RPC is `SECURITY DEFINER` with explicit empty `search_path`, schema-qualified business SQL, no dynamic SQL, private-helper execution revoked, public RPC execution revoked from anonymous, and narrow authenticated execution. Base Candidate/document tables remain unavailable to browsers.

## NONPROD corrective migrations

Migration 024 corrected a return-structure mismatch in `list_candidate_portal_interviews(integer, integer)`. Canonical `interviews.interview_round` remains `smallint`; the Candidate RPC preserves its reviewed text contract through an explicit `i.interview_round::text` projection. Candidate tenancy, pagination, grants, and privacy behavior are unchanged.

Migration 025 corrected two defects found during dedicated NONPROD validation. It replaced UID-folder-only Candidate Storage authorization with the combined active/unambiguous Candidate-linkage and matching-UID-path requirement, while adding a separate Admin-verifier SELECT-only policy. It also implemented the controlled `Uploaded -> Under Verification -> Verified` or `Re-upload Required` lifecycle, with distinct review-start and final-review actor/timestamp attribution. These were NONPROD validation findings, not production incidents.

## Final runtime and browser evidence

Rollback-scoped checkpoints 025, 026, 027, and 028 pass. Required W2-W5, migration-015, and legacy/public backend regressions pass, and the final frontend result is 121/121. Runtime coverage proves server-derived Candidate identity, Candidate A/B isolation, portal separation, compulsory Mobile/Aadhaar validation, duplicate Aadhaar privacy, Resume-aware profile completion, opportunity-state filtering, canonical duplicate-safe applications, read-only Candidate interview/joining boundaries, document replacement/review, private Storage policy boundaries, Admin-only verification, conditional UAN/ESIC validation, joining-checklist invariants, override authorization/audit, and sensitive-data suppression.

Live localhost validation separately passed anonymous route containment; Candidate dashboard, profile, masked identity, document inventory, private Storage upload/replace, opportunities, applications, interviews, joining, logout/back/BFCache containment; Candidate B isolation; Company-only and Contractor-only Candidate Portal denial; Candidate-to-Admin denial; Admin Candidate inventory and document review; desktop smoke; and 400px responsive smoke. A focused final Admin retest visibly proved Uploaded -> Start Review -> Under Verification -> Mark Verified -> Verified. Under Verification exposed Mark Verified and Request Re-upload, the final Verified row exposed View Securely only, and the canonical server inventory refreshed after each transition. The authorized synthetic Admin populated `review_started_at/by`, `reviewed_at/by`, and `verified_at/by` correctly. The generic first-time authenticated onboarding case is intentional, not a security defect, and grants no protected existing Candidate scope before successful onboarding. Immediate-joining and UAN/ESIC checkbox alignment were recorded as minor non-blocking presentation observations.

## Final fixture cleanup evidence

Manifest-bound cleanup deleted three potentially sensitive browser-created Storage objects without opening, downloading, previewing, parsing, hashing, or creating signed URLs for their contents. It then removed nine attributable audit rows, seven Candidate document rows, one interview, one joining, two application-history rows, one application, onboarding/preferences, the dedicated requirement, Candidate A/B, Company/Contractor denial fixtures, four platform users, the internal Admin/bootstrap fixture, and all six Auth users. Final Auth users, identities, sessions, database fixtures, and Storage residue are zero; orphan and cross-tenant checks pass; the unrelated staging baseline is unaffected. The ignored manifest and temporary browser copy were removed, the loopback server was stopped, and port 4176 was closed.

The focused review-start retest used two additional NONPROD Auth users and the minimum Candidate/Admin/document fixture. Cleanup removed both Auth users, their identities and session, the document-review audit rows, and all database fixture records. Auth, identity, session, database, audit, and Storage residue are zero; orphan and cross-tenant verification pass. Its ignored manifest and temporary browser copy were removed, the loopback server was stopped, and port 4177 was closed. The private bucket, five migration-025 Storage policies, migration-024 correction, and migrations 023-025 remain intact.

## Testing and limitations

Checkpoint `025_candidate_portal_foundation_test.sql` is transaction- and rollback-scoped. It covers initial onboarding and mandatory-field rejection, Candidate A/B isolation, inactive/non-member/other-portal boundaries, Aadhaar masking/fingerprinting, profile/preferences and masked bank/PF/ESIC persistence, Open/Public opportunity filtering (including pre-approval Contractor exclusion), canonical and duplicate-safe application creation, own interview/joining projections, document registration/replacement and privacy/review roles, joining checklist, Admin override, direct-table denial, grants/storage-policy posture, and audit redaction. Focused checkpoints 026 and 027 cover the interview projection correction and hardened Storage/document-review lifecycle; broad checkpoint 028 closes the remaining Candidate runtime matrix. Frontend tests cover route/session boundaries, BFCache, responsive/accessibility foundation, private uploads, masked identity, canonical application use, read-only progress, and prohibited patterns.

Deferred: real OTP, antivirus/content scanning and quarantine, checksums, retention/deletion automation, Company/Contractor Resume release, OCR, external Aadhaar/PAN/bank/PF/ESIC validation, PF/ESIC filing, Candidate interview rescheduling, WhatsApp, AI matching, payroll, attendance, billing, mobile apps, and deployment. Storage scanning and stronger key-managed fingerprinting require separately reviewed infrastructure.
